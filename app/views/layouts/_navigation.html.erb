<%# navigation styled for Bootstrap 3.0 %>
<nav class="navbar navbar-inverse navbar-fixed-top">
	<div class="container">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<%= link_to t('admin.navigation.home'), admin_path, class: 'navbar-brand' %>
		</div>
		<div class="collapse navbar-collapse">
			<ul class="nav navbar-nav">
				<% if admin_signed_in? %>
				<li><%= link_to t('admin.navigation.applications'), admin_applications_path %></li>
				<li><%= link_to t('admin.navigation.sign_out'), destroy_admin_session_path, :method=>'delete' %></li>
				<% else %>
				<li><%= link_to t('admin.navigation.sign_in'), new_admin_session_path %></li>
				<% end %>
			</ul>
		</div>
	</div>
</nav>
